# 自动剪辑模块设计方案

> 变更记录（2026-09）：本方案已收敛为**纯本地语义剪辑**——旧版 MoneyPrinterTurbo
> 任务流水线（Pexels/Pixabay/Coverr 等远程素材源、WaveSpeed/Seedance/OFox/MuAPI/
> 秘塔等 AI 生成素材、自动生成文案、Streamlit WebUI 与 CLI）已整体删除，仅保留
> "人工文案 → 本地素材语义匹配 → 合成"一条链路。素材新增 `userNotes` 人工备注
> 参与向量索引与标签召回；候选重排序对模型返回的池外/重复 ID 静默容错；模型
> 结构化调用统一 `temperature: 0` 并按候选数估算 `max_tokens`；文案断句兼容英文
> 句点。详见 canvas-two/auto-video 与 canvas-two/server/src/auto-video。
> 管理页面新增"素材存储位置"设置：存储位置以剪辑引擎 config.toml 的
> `[storage] local_videos_dir` 为事实源，新上传素材写入所选目录，历史素材
> 仍从默认目录回退可读（预览/探测/渲染统一走多目录查找）。

面向 Windows 客户端的新增模块：影棚无声产品素材 + 人工文案 → 自动匹配画面 → 粗剪成片。与画布模块完全解耦，互不影响。

## 1. 需求定义

- 已有大量影棚产品拍摄素材，无音频。
- 由人提供字幕文案（口播稿/卖点文案）。
- 系统自动为每句文案匹配最合适的素材画面，按时长裁切拼接，输出粗剪视频。
- 可选：TTS 旁白、字幕烧录、BGM、转场。

对标产品形态：剪映"图文成片"、一帧秒创、腾讯智影。

## 2. 总体架构（壳 + 独立模块）

客户端采用"壳应用 + 模块"结构，壳只负责：窗口/进程管理、登录态、左侧导航、本地服务编排。

```
shell（壳）
├── 登录 / 用户 / 主题 / 布局
├── 模块注册表（路由 + 导航项 + 图标）
└── 本地服务管理（后端进程、ffmpeg 进程）

modules/
├── canvas        现有画布，原封不动
├── video-edit    自动剪辑（本方案）
└── knowledge     公司 AI 知识库
```

### 解耦原则

- 前端：每个模块独立目录，React.lazy 按路由加载；zustand store 各模块自建，禁止跨模块 import。共享物收敛到 shared/：API client（带 token 的 axios 实例）、认证态、基础 UI 组件。只共享基础设施，不共享业务逻辑。
- 后端：保留单一 Express 服务，按命名空间挂载路由：
  - /api/canvas/* 现有路由不动
  - /api/video/* 剪辑模块（素材、任务、时间线、导出）
  - /api/kb/* 知识库模块
- 数据：Prisma schema 按模块分区，表名加前缀（Video*、Kb*）。跨模块共享的表只有 User / Session / ModelChannel / MediaFile。
- 效果：删掉 modules/video-edit 目录 + 后端 /api/video 挂载，项目照常编译运行。

### 客户端外壳选型

- 前端为标准 Vite SPA，套 Electron（主进程拉起 server + ffmpeg），React 代码零改动。
- 后续可评估 Tauri 以减小体积。

## 3. 剪辑流水线

素材入库打标 → 文案拆解 → 画面匹配 → 时长对齐 → 合成导出

### 3.1 素材入库与打标（一次性，决定匹配上限）

1. 镜头切分：长素材先按画面切换点自动切成"一镜头一段"。工具：PySceneDetect 或 ffmpeg scene detect。
2. 抽帧：每段镜头均匀抽 2-4 帧。
3. 视觉打标：帧图送视觉大模型，输出结构化描述：产品名称/品类、景别（特写/中景/全景）、动作（旋转/手持/摆放/使用演示）、场景（白底/实景/影棚灯光）、画面质量与可用性标记。
4. 入库：描述文本 + 向量 embedding 落库（PostgreSQL + pgvector）。
5. 人工校准：客户端提供素材管理页，可人工修正标签、预览镜头。修正后的标签持续提升后续匹配质量。

素材打标与知识库模块共用 pgvector + embedding + 检索地基，一次搭建两个模块受益。

### 3.2 文案拆解

- 人工输入文案 → DeepSeek 拆成分镜清单。
- 每句输出：文本、画面需求（景别/动作/产品部位）、预估时长。
- 用 JSON schema / structured output 约束返回格式。

### 3.3 画面匹配

- 每句的画面需求 → 向量检索召回候选镜头 top 5 → DeepSeek 精排选最优。
- 素材量小（几百段以内）可跳过 embedding，直接把素材描述清单塞 prompt 让 DeepSeek 一次性匹配。
- 产出时间线 JSON：

```json
[
  {
    "clipId": "镜头素材ID",
    "inPoint": 0.0,
    "outPoint": 3.2,
    "subtitle": "这款保温杯采用316不锈钢内胆",
    "reason": "特写+内胆展示，匹配度最高"
  }
]
```

- 时间线支持人工在客户端里微调（换镜头、拖动长短）后再合成。

### 3.4 时长对齐

- 有旁白：按句调用 TTS 生成配音，每句音频时长 = 该句画面时长，音画天然对齐。
- 无旁白：按中文阅读速度估算字幕时长（约 4-5 字/秒），画面裁至同长。
- 素材短于所需时长：慢放（不超过 0.8x）或循环补足；过长则截取最佳片段。

### 3.5 合成导出

- ffmpeg：按时间线裁切拼接、烧录字幕、垫 BGM、简单转场（淡入淡出/叠化）。
- 客户端内 spawn 本机 ffmpeg 二进制，无授权问题。
- 进阶：用 Remotion（React 代码视频模板）做文字动效、画中画、品牌包装，与前端技术栈同源，Node 侧渲染导出。

## 4. 技术选型清单

| 环节 | 选型 | 说明 |
| --- | --- | --- |
| 镜头切分 | PySceneDetect / ffmpeg scene detect | 入库预处理 |
| 视觉打标 | Qwen-VL（DashScope 按量）或本地 Qwen2.5-VL | 量大选本地省钱 |
| 文案拆解 / 匹配决策 | DeepSeek API | 便宜、中文好、OpenAI 兼容协议 |
| 向量 embedding | 阿里 text-embedding-v4 或本地 BGE | DeepSeek 无 embedding 接口 |
| 向量存储 | PostgreSQL + pgvector | 与知识库模块共用 |
| TTS（可选） | edge-tts 免费起步，正式用火山引擎 | 中文音色质量好 |
| 合成执行 | ffmpeg（fluent-ffmpeg / spawn） | 第一版主力 |
| 模板化包装（进阶） | Remotion | React 技术栈复用 |
| 本地剪辑服务 | 独立 Node 进程（复用 canvas-agent 模式） | 前端只下任务、看进度、取结果 |

## 5. 数据模型草案（Prisma，video 命名空间）

- VideoMaterial：原始素材（关联 MediaFile，时长、分辨率、入库状态）
- VideoShot：切分后的镜头（所属素材、入点/出点、缩略图、描述文本、embedding、人工校准标记）
- VideoScript：文案（原始文本、拆分后的句子 JSON）
- VideoTimeline：时间线（句子到镜头的匹配结果 JSON、版本、状态：自动匹配/人工调整/已导出）
- VideoExportTask：导出任务（参数、进度、输出文件、状态）

## 6. 后端接口草案（/api/video/*）

- POST /api/video/materials 上传素材（触发切分+打标异步任务）
- GET /api/video/shots 镜头列表（按产品/景别筛选）
- PATCH /api/video/shots/:id 人工修正标签
- POST /api/video/scripts/:id/match 文案匹配，生成时间线
- PUT /api/video/timelines/:id 人工调整时间线
- POST /api/video/timelines/:id/export 提交合成任务
- GET /api/video/exports/:id 查询导出进度/下载

异步任务（切分、打标、合成）走独立 worker 进程，模式参考现有 server/src/worker.ts。

## 7. 前端页面（modules/video-edit）

- 素材库：素材/镜头网格、标签筛选、标签编辑
- 创作页：左侧文案编辑（分句预览），右侧自动匹配的时间线（镜头卡片 + 可替换候选 + 拖拽调序/调长）
- 导出页：合成进度、历史成片、下载/重新导出

## 8. 落地节奏

| 阶段 | 内容 | 目标 |
| --- | --- | --- |
| P1（2-3 周） | 镜头切分 + VLM 打标 + DeepSeek 匹配 + ffmpeg 粗剪 | 选文案到出粗剪全流程跑通 |
| P2 | 时间线人工微调界面、字幕样式、BGM、TTS 旁白 | 成片可用度提升 |
| P3 | Remotion 模板包装、批量出片、素材标签体系运营化 | 规模化生产 |

## 9. 风险与注意

- 匹配质量上限在素材元数据，不在算法：务必做镜头切分 + 人工校准入口。
- LLM 只做决策，不碰视频文件；执行层永远是 ffmpeg。
- 项目为 AGPL-3.0：公司内部使用无碍，对外分发/商用需注意传染性，二开保留原作者信息与前端标识。
- 视觉打标调用成本：量大时优先本地 Qwen2.5-VL，云端按量只适合起步验证。
