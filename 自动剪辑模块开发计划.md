# 自动剪辑模块开发计划（v1：AI 拆句 + 轻量打标 + LLM 选片）

> 本文档是落地执行计划，与《自动剪辑模块设计方案.md》配套：设计文档讲整体架构与技术选型，本文档讲每个阶段做什么、做到什么程度算过。
> 已定关键决策：AI 介入档位为"轻量打标 + LLM 选片"（素材量小，不上镜头切分与向量检索，留作 v2 增强）。

## 1. 目标与范围

**目标**：用户选择货号 + 上传文案 → DeepSeek 拆句分镜 → 从"货号素材 + 通用素材"候选池中由 DeepSeek 为每句选出最匹配的画面并定截取区间 → ffmpeg 合成带字幕的粗剪成片。

**v1 范围（做）**：
- 素材管理：上传、货号/分类必填、VLM 自动打标（人工可改）、分类 CRUD
- 文案：粘贴或 .txt 上传、AI 拆句分镜
- 匹配：AI 选片（货号素材优先、通用素材兜底，由 system prompt 约束），AI 失败回退规则匹配
- 导出：ffmpeg 合成、字幕烧录、竖屏统一、可选 BGM、任务进度
- 时间线预览与基础微调（替换片段、改时长、删除片段）

**v1 不做（留 v2+）**：镜头自动切分、向量检索、TTS 旁白、.docx 导入、Remotion 模板包装、批量出片、横屏以外的分辨率矩阵、打标计费。

## 2. 总体架构

落地于 canvas-two 仓库，新增模块与画布、知识库完全解耦：

```
canvas-two
├── server/src/video/          新增：路由、服务、worker 任务
│   └── Express 挂载 /api/video/*
├── server/prisma/schema.prisma 追加 Video* 表（仅关联 User / MediaFile）
└── web/src/modules/video-edit/ 新增：三页面 + store（路由懒加载）
```

**原则**：
- 后端保留单一 Express 服务，`/api/video/*` 命名空间，zod 校验 + access 权限检查，模式对齐现有 kb 模块
- 前端复用 shared/ 的 API client（带 token）、认证态、基础 UI 组件，业务 store 模块自建，禁止跨模块 import
- LLM 走现有 ModelChannel 配置（OpenAI 兼容协议）：DeepSeek（拆句 + 选片）、DashScope Qwen-VL（素材打标）
- **LLM 只做决策，不碰视频文件；执行层永远是 ffmpeg**（Node spawn 本机二进制）
- 删 modules/video-edit + 后端 /api/video 挂载后，项目照常编译运行

## 3. 核心规则

### 3.1 素材属性
- **货号**（必填）：自由文本，不建唯一约束；输入框提供已有货号联想
- **分类**（必填，单选）：种子三条——文胸 / 内裤 / 通用；用户可新建、改名、删除
- **"通用"分类内置**：不可删除、不可改名，防止兜底池被误删；其余分类有素材时禁止删除（409）

### 3.2 VLM 打标
- 素材上传后异步触发：抽 2 帧送 Qwen-VL，structured output 返回：
  - `description`：一句话画面描述（如"白底上文胸旋转展示，特写罩杯面料"）
  - `tags`：结构化标签，枚举值——景别（特写/中景/全景）、动作（旋转/手持/上身/平铺）、场景（白底/实景/影棚）
- 打标状态机：`pending → ok / failed`；failed 可在素材库手动重试
- 素材库支持人工修改 description 与 tags，修正结果直接用于后续匹配
- 渠道不可用时**不阻塞上传**，素材停在 failed 状态

### 3.3 AI 拆句
- DeepSeek structured output 返回 JSON，每句含：
  - `text`：字幕文本
  - `estimatedDuration`：预估时长（参考 4.5 字/秒）
  - `shotNeed`：画面需求描述（如"特写 + 面料细节"）
- LLM 可合并过短句、拆分长句，比分标点规则更有"分镜感"
- 后端对 estimatedDuration 做 clamp：[1.5s, 12s]，防模型异常值

### 3.4 AI 选片
- 候选池 = 货号精确匹配素材 + 分类=通用素材，按上传时间排序，**上限 100 段**（防 prompt 超限，超出截断并提示）
- 每段素材携带 description + tags 清单进入 prompt
- 一次调用让 DeepSeek 为每句输出：`clipId` + `inPoint/outPoint` + `reason`
- 兜底约束写进 system prompt：货号素材优先，货号不足时用通用素材补足
- **失败回退**：LLM 调用失败/超时/JSON 校验失败（重试 1 次后仍失败）→ 自动回退规则匹配（货号按序 + 通用轮替，句数多于片段数时整表循环），clips 标 `matchSource: "rule"`，UI 显示"规则匹配（AI 不可用）"徽标
- 单句选不到素材时同样回退规则分配
- 目标：**LLM 全链路失败也能出片**

### 3.5 时长对齐
- 每句画面时长 = AI 预估时长（经 clamp）
- 素材 ≤5s 短于所需：慢放至最低 0.8x，仍不足则循环该素材补足
- 素材长于所需：从 inPoint 截取

### 3.6 合成规则
- 统一竖屏 1080×1920：素材比例不符时居中 + 模糊背景板（或纯色底），不拉伸变形
- 字幕烧录：服务端按时间线生成 .ass 控制样式（位置/大小/描边）
- BGM 可选：用户上传音频，循环垫底，volume 压至 0.25
- 输出 H.264；导出任务异步执行，`-progress` 回写进度，失败落 error 字段支持重试

## 4. 数据模型（Prisma 新增，Video 命名空间）

```prisma
model VideoCategory {
  id        String          @id @default(cuid())
  name      String
  sort      Int             @default(0)
  isBuiltin Boolean         @default(false) // "通用"=true，不可删改
  materials VideoMaterial[]
}

model VideoMaterial {
  id               String   @id @default(cuid())
  ownerId          String
  sku              String   // 货号，必填
  categoryId       String
  mediaFileId      String   // 关联 MediaFile
  thumbnailMediaId String?
  duration         Float    // ffprobe 探测（秒）
  width            Int
  height           Int
  description      String   @default("")
  tags             String[] @default([])
  taggingStatus    String   @default("pending") // pending/ok/failed
  createdAt        DateTime @default(now())
}

model VideoScript {
  id        String   @id @default(cuid())
  ownerId   String
  title     String
  rawText   String
  sentences Json     // [{text, estimatedDuration, shotNeed}]
}

model VideoTimeline {
  id       String @id @default(cuid())
  scriptId String
  clips    Json   // [{materialId, inPoint, outPoint, speed, subtitle, matchSource, reason}]
  status   String @default("auto") // auto/adjusted/exported
}

model VideoExportTask {
  id           String  @id @default(cuid())
  timelineId   String
  params       Json    // {bgmMediaId?, resolution, subtitleStyle}
  progress     Int     @default(0)
  outputMediaId String?
  status       String  @default("pending") // pending/running/succeeded/failed
  error        String?
}
```

## 5. 接口设计（/api/video/*）

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | /categories | 分类列表（含 isBuiltin） |
| POST | /categories | 新建分类（重名校验） |
| PATCH | /categories/:id | 改名/排序（builtin 禁改） |
| DELETE | /categories/:id | 删除（有素材 409；builtin 403） |
| POST | /materials | multipart 上传，ffprobe 探测，触发异步打标 |
| GET | /materials | 列表，筛选 sku / categoryId / taggingStatus |
| PATCH | /materials/:id | 改货号/分类/description/tags，触发重打标开关 |
| DELETE | /materials/:id | 删除（清 DB + 磁盘文件） |
| POST | /scripts | 创建文案（rawText 粘贴或 .txt） |
| GET | /scripts | 文案列表 |
| GET | /scripts/:id | 文案详情（含拆句结果） |
| POST | /scripts/:id/match | 拆句 + 选片，生成时间线，返回 clips |
| PUT | /timelines/:id | 保存人工微调 |
| POST | /timelines/:id/export | 提交合成任务 |
| GET | /exports/:id | 导出进度/结果 |
| GET | /exports | 导出历史 |

## 6. 前端页面

1. **素材库**：分类侧栏（含"新建分类"）；素材卡片网格（缩略图 + 货号 + 分类 + 描述 + 打标状态徽标 pending/ok/failed + 失败重打按钮）；上传弹窗（货号/分类必填、批量）；编辑与删除
2. **创作页**：货号联想输入 + 文案编辑框/.txt 上传 → "生成预览" → 时间线卡片（缩略图 + 字幕 + 时长 + 匹配理由 + "AI/规则"来源徽标，点击可换候选片段）
3. **导出页**：进度条、历史成片列表、下载、失败重试

## 7. 阶段划分与验收标准

### P0：骨架与数据层（约 2 天）
- 追加 5 张 Prisma 表并 `db push`；种子分类文胸/内裤/通用（通用 isBuiltin=true）
- `server/src/video/` 路由骨架：zod 校验、access 检查，对齐 kb 模块模式
- 前端模块目录 + 路由 + 导航项（懒加载）
- 确认 ModelChannel 配置中 DeepSeek 与 DashScope(Qwen-VL) 渠道可用
- 同步更新《自动剪辑模块设计方案.md》落地节奏为本计划
- **验收标准**：
  - `prisma db push` 无报错，DB 中三条种子分类存在
  - `GET /api/video/categories` 未登录返回 401
  - 前端三页可空态打开
  - 画布模块与知识库模块功能回归无异常

### P1：素材与分类管理 + VLM 打标（约 5 天）
- 分类 CRUD：重名校验、占用禁删 409、"通用"保护
- 素材上传：mp4/mov 校验、ffprobe 探测时长/分辨率、缩略图抽帧、货号+分类必填、批量上传
- 打标 worker：抽 2 帧送 Qwen-VL，structured output 校验，失败可重试；列表显示 pending/ok/failed 徽标
- 素材列表：按货号/分类/打标状态筛选；编辑货号/分类/描述/标签；删除级联清理 DB 与磁盘
- **验收标准**：
  - 上传 20 段素材：3 秒内入库显示 pending，1 分钟内全部打标完成；描述可读、标签属于预定义枚举
  - 缺货号/缺分类/错误格式上传被拦截，返回中文可读错误
  - 打标失败段显示 failed，重试后成功
  - 修改描述/标签保存生效并用于后续匹配
  - 删除被占用分类返回 409；删除素材后磁盘与 DB 同步清理
  - 断网/渠道不可用时不阻塞上传，素材停在 failed

### P2：AI 拆句与选片（约 5 天）
- 拆句模块：prompt 构造、structured output 校验、失败重试 1 次
- 选片模块：候选池组装（≤100 段）、prompt 构造、结果校验（clipId 合法性、区间合法性）、重试 1 次
- 规则兜底模块：货号按序 + 通用轮替，句数 > 片段数整表循环
- 创作页联调：货号联想 → 文案粘贴/上传 → 生成预览 → 时间线卡片
- 边界：空候选池 422；候选 >100 截断提示；单句落选回退规则
- **验收标准**：
  - 构造"货号 A 3 段（含 1 段特写面料）+ 通用 2 段"，输入含"面料细节"卖点的文案 → AI 将面料句分配给特写段，reason 提及面料/特写
  - 其余句均在候选池内且货号素材优先
  - 模拟 LLM 超时 → 回退规则匹配，显示"规则匹配"徽标，流程不中断
  - 句数 > 片段数时 AI 复用片段且不报错
  - JSON 校验失败触发重试，最终给可读错误而非白屏
  - taggingStatus=failed 的素材仍参与规则兜底

### P3：合成导出（约 5 天）
- ffmpeg 管线：trim/setpts（慢放最低 0.8x，不足循环）/concat → 1080×1920 模糊背景板 → .ass 字幕烧录 → 可选 BGM 循环 + volume 0.25 → H.264
- 导出任务异步（worker 模式），`-progress` 回写进度，失败落 error 可重试
- 导出页：进度轮询、历史列表、下载
- **验收标准**：
  - 成片时长误差 ≤0.3 秒；字幕与对应画面同步出现
  - 竖屏无拉伸变形；慢放片段播放顺滑
  - BGM 音量正常不盖字幕
  - 合成中途杀进程 → failed 且重试成功
  - 连续导出 3 次结果一致

### P4：打磨与微调（约 3 天）
- 时间线微调：片段替换（候选池可选）、单段改时长（联动下一段）、删除片段
- 导出参数：分辨率切换（1080×1920 / 1920×1080）、字幕大小/位置、BGM 开关记忆
- 成本护栏：打标/拆句/选片调用计数与失败率先落日志
- 性能：200 段素材内列表筛选 <500ms
- **验收标准**：
  - 微调后重导出生效、时长重算正确
  - 导出参数记忆刷新后保留
  - 200 段素材压测达标
  - P0–P3 验收用例全量回归通过

## 8. 测试计划

- **单元测试（vitest，与 server 现有测试一致）**：
  - prompt 构造与 response 解析（含脏 JSON 修复）
  - 规则兜底匹配（货号按序 + 通用轮替 + 循环）
  - 时长/慢放计算（0.8x 边界、循环补足）
  - 候选池组装与截断
- **接口测试**：分类/素材/文案/匹配/导出各接口正常流 + 权限流（未登录 401、跨用户 403）+ 校验错误流；LLM 渠道异常时 match 接口返回 rule 兜底结果
- **手动验收**：按 P0–P4 各阶段验收清单执行；重点回归项：AI 失败回退、打标失败重试、素材删除级联、空候选池提示、导出可重试

## 9. 假设与默认决策

- **AI 档位**：轻量打标 + LLM 选片。素材量小（每货号几十段），不上镜头切分与向量检索；VLM 用 DashScope Qwen-VL 按量付费（几分钱/段）；素材量大后再评估本地 Qwen2.5-VL + pgvector（知识库模块已备地基）
- **LLM 调用**：拆句与选片各一次调用，同走 DeepSeek；JSON 校验失败重试 1 次后整体回退规则匹配
- **时长**：AI 预估 + 后端 clamp [1.5s, 12s]；素材不足慢放至 0.8x 再循环
- **文案输入**：粘贴文本框 + .txt 上传；.docx 留 v2
- **货号**：自由文本、不唯一、输入联想
- **成片**：默认竖屏 1080×1920；横屏 1920×1080 作为导出参数
- **TTS**：v1 不做（素材无声、≤5s，成片由字幕 + BGM 驱动）；需要口播风时 v2 评估 edge-tts / 火山引擎

## 10. 风险与注意

- **匹配质量上限在素材元数据**：VLM 打标 + 人工校准入口是质量根基，P1 不可省略
- **LLM 只决策不执行**：执行层永远 ffmpeg，AI 失败可降级，保证永远能出片
- **成本**：打标按段计费、拆句/选片按篇计费，P4 加调用计数埋点；素材量大时切本地 VLM
- **AGPL-3.0**：公司内部使用无碍；对外分发/商用需注意传染性，二开保留原作者信息与前端标识
