# AI 公司知识库模块设计方案

## 1. 模块定位

在公司内部搭建一个"经验型知识库"：员工以论坛形式发布工作心得、踩坑记录、标准工作流程；AI 助手接入这些内容，员工可以像聊天一样提问，AI 基于全员的沉淀做语义检索并**综合多位作者的经验生成答案**，而不是简单的查找复制。

两个核心使用场景：

- 人找知识：浏览/搜索/按分类看帖，像逛论坛。
- 知识找人：直接向 AI 提问"客户退款的完整流程是什么"，AI 检索相关帖子并融合成一份可执行的回答，附引用来源。

## 2. 技术选型（贴合现有栈）

| 层 | 选择 | 理由 |
|---|---|---|
| 前端 | 复用 `web/`：React 19 + Vite + shadcn + Tailwind | 与画布项目同一套 UI 体系 |
| 后端 | 复用 `server/`：Express 5 + Prisma + zod | 同一进程加路由模块即可，无需独立服务 |
| 数据库 | 现有 PostgreSQL + `pgvector` 扩展 | 一份库存结构化数据和向量，运维成本最低 |
| 对象存储 | 现有 MinIO | 帖子附件（图片、文档、流程图） |
| Embedding / LLM | 可配置的 OpenAI 兼容接口 | 与公司现有 AI 基建对齐，方便换国产模型 |
| 异步任务 | 复用现有 worker（`start:worker`） | 帖子入库后的切分、向量化放后台做 |

## 3. 总体架构

```
发帖 -> 审核/直接发布 -> 结构化存储(Postgres)
                      └-> 异步任务：清洗 -> 切分 -> Embedding -> 向量入库(pgvector)

提问 -> 意图理解/查询改写 -> 混合检索(关键词 + 向量) -> Rerank ->
      取 Top-N 片段 + 引用 -> LLM 综合生成 -> 流式返回答案 + 来源列表
```

关键原则：**帖子是唯一的知识源**，AI 不直接拿对话内容当知识。对话中确认有用的结论，引导员工"沉淀为帖子"，形成闭环。

## 4. 数据模型（Prisma 草案）

```prisma
model KbCategory {
  id    String   @id @default(cuid())
  name  String
  sort  Int      @default(0)
  posts KbPost[]
}

model KbPost {
  id         String   @id @default(cuid())
  authorId   String
  categoryId String?
  title      String
  type       String   // insight=心得 / workflow=流程 / qa=问答
  content    String   // Markdown
  tags       String[]
  status     String   // draft / published / archived
  viewCount  Int      @default(0)
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
  comments   KbComment[]
  chunks     KbChunk[]
}

model KbComment {
  id       String  @id @default(cuid())
  postId   String
  authorId String
  content  String
  parentId String? // 楼中楼
}

model KbChunk {
  id        String                   @id @default(cuid())
  postId    String
  ord       Int                      // 片段序号
  heading   String?                  // 所属小节标题，提升检索命中率
  content   String
  embedding Unsupported("vector(1024)")? // pgvector
  updatedAt DateTime                 @updatedAt
}

model KbConversation {
  id       String      @id @default(cuid())
  userId   String
  title    String
  messages KbMessage[]
}

model KbMessage {
  id        String  @id @default(cuid())
  convId    String
  role      String  // user / assistant
  content   String
  citations Json?   // [{postId, chunkId, title, excerpt}]
  feedback  Int?    // 1 有用 / -1 无用，用于质量回流
}
```

说明：

- `KbChunk` 是检索的基本单位，一篇帖子切成多个 chunk；帖子编辑后整篇重新切分并向量化（简单可靠，公司内容量级下性能足够）。
- `citations` 存引用快照，保证答案可追溯。
- 点赞/收藏可用一张通用的 `KbReaction` 表，不展开。

## 5. RAG 管线设计（本模块的核心）

### 5.1 入库管线（写入侧）

1. 帖子发布后触发 worker 任务。
2. 清洗：去掉无意义装饰字符，保留 Markdown 结构。
3. 切分：按 Markdown 标题层级优先切分，单 chunk 控制在 300~800 字，chunk 之间保留少量重叠（约 10%）；每段记录所属小节标题。
4. 调用 embedding 接口批量向量化，写入 `pgvector`，建 HNSW 索引。
5. 帖子更新/归档时同步失效旧 chunk。

### 5.2 检索管线（读取侧）

1. **查询改写**：结合对话上下文把用户问题改写成独立检索语句（解决"那第二步呢"这类指代）。
2. **混合检索**：
   - 向量检索：pgvector 余弦相似度 Top-K；
   - 关键词检索：Postgres `tsvector` 全文索引（流程名、专有术语命中率高）；
   - 两者结果按 RRF（倒数排名融合）合并。
3. **Rerank**（可选二期）：用 rerank 模型对合并结果精排，取 Top 5~8。
4. **综合生成**：把选中片段连同"帖子标题 + 作者 + 时间"一起进 prompt，明确要求：
   - 综合多篇内容，不得整段照抄；
   - 观点冲突时列出分歧并标注来源；
   - 每条关键结论附引用编号；
   - 检索结果不足以回答时明确说"知识库暂无相关内容"，不要编。
5. **流式输出**（SSE），回答末尾附来源帖子卡片，可点击跳转到原帖。

### 5.3 质量与运营机制

- 反馈回流：回答下方"有用/没用"按钮，写入 `KbMessage.feedback`，定期导出低分会话做 badcase 分析。
- 权威加权：官方认证流程（管理员标记 official）在检索时加权。
- 时效处理：检索时对超过一定时间的帖子降权；回答中标注"该经验来自 X 月前，请留意时效"。
- 去重提醒：发帖时先做向量相似度检测，提示"已有相似帖子"，引导补充评论而非重复发帖。

## 6. API 设计（挂在现有 server 下）

```
# 论坛
GET    /api/kb/posts?category=&tag=&keyword=&sort=   列表/搜索
POST   /api/kb/posts                                 发帖
GET    /api/kb/posts/:id                             详情（浏览量+1）
PATCH  /api/kb/posts/:id                             编辑（触发重切分）
DELETE /api/kb/posts/:id                             归档
POST   /api/kb/posts/:id/comments                    评论
GET    /api/kb/categories                            分类

# AI 对话
POST   /api/kb/chat/conversations                    新建会话
POST   /api/kb/chat/conversations/:id/messages       提问（SSE 流式）
GET    /api/kb/chat/conversations/:id                会话历史
POST   /api/kb/chat/messages/:id/feedback            有用/没用

# 管理
GET    /api/kb/admin/stats                           命中率、反馈分布
POST   /api/kb/admin/posts/:id/pin-official          标记官方流程
```

权限：复用现有登录态；发帖/评论全员开放，分类管理、官方标记、统计面板仅管理员。

## 7. 前端页面规划

- `/kb`：知识库首页。左侧分类/标签栏，中间帖子流（卡片：标题、类型徽章、摘要、作者、热度），顶部搜索框。
- `/kb/post/:id`：帖子详情。Markdown 渲染 + 评论区 + 相关推荐（向量相似帖）。
- `/kb/new`：发帖编辑器。类型选择（心得/流程）、Markdown 编辑预览、标签输入、相似帖提醒。
- `/kb/chat`：AI 对话。左会话列表，右聊天区；回答中的引用编号悬浮显示来源片段，点击跳原帖；"这个回答有用，沉淀为帖子"一键转帖入口。
- 入口复用：聊天面板做成全局抽屉，画布页面也能随时唤起提问。

## 8. 实施分期

**一期（MVP，约 2~3 周）**

1. 数据表 + pgvector 扩展 + 迁移。
2. 论坛 CRUD：列表、发帖、详情、评论、分类。
3. 入库管线：切分 + embedding + 向量入库。
4. AI 对话：查询改写 + 混合检索 + 综合生成 + SSE + 引用展示。
5. 反馈按钮。

**二期（质量提升）**

6. Rerank 精排、官方流程加权、时效降权。
7. 发帖相似度去重提醒、"回答沉淀为帖子"。
8. 附件上传（MinIO）与文档解析入库（PDF/Word 提取文本后走同一管线）。
9. 管理统计面板：检索命中率、零命中问题榜（指导补内容）。

**三期（深化）**

10. 与画布联动：AI 回答可直接生成流程图卡片插入画布。
11. 权限分区（按部门可见性）、周报自动生成（汇总本周新增经验）。

## 9. 风险与注意事项

- **冷启动内容少**：AI 回答质量依赖帖子量。上线前先让各部门人工导入 20~50 篇存量流程文档，避免"问啥都没有"打击使用信心。
- **成本**：embedding 按 token 计费很低；对话生成是主要成本，可做每用户每日限额 + 缓存高频问题的答案。
- **幻觉控制**：prompt 强制"无依据不回答" + 引用必现，是最有效的两道闸。
- **隐私**：知识库默认全员可见；含敏感信息的内容靠发帖时的类型/分区约定，三期再做细粒度权限。
- **中文分词**：`tsvector` 中文需要 `zhparser` 扩展，或用 `pg_trgm` 做模糊匹配兜底，部署时确认。
